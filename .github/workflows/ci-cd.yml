name: cicd

on:
  push:
    branches:
      - main

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile code
        run: echo "Compile completed successfully"

  security-check:
    needs: compile
    runs-on: ubuntu-latest

    steps:
      - name: Security check
        run: echo "Security scan completed successfully"
